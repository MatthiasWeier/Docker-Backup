# Docker-Backup
Getting a docker Backup to tar files over DockerCompose.yml
This Script only works once downloaded "sudo cpanm File::Copy::Recursive"
Paths are hardcoded. NEED TO CHANGE!!
Written on: Raspi OS
